# Báo cáo đóng góp cá nhân

## Thông tin

- Họ và tên: Phạm Anh Minh
- Mã học viên: 2A202603009
- Nhóm: onedayonename
- Repository/branch: `K4-L3B-RAG-Pipeline` / `minh`
- Nhánh remote: `origin/minh`

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 7 - RRF reranking | Triển khai Reciprocal Rank Fusion cho nhiều ranked list, loại kết quả trùng theo ID, tính điểm theo công thức `1 / (k + rank)`, sắp xếp kết quả và giới hạn `top_k`. | `src/task7_reranking.py`, commit `d6c9c65` | Hoàn thành |
| Task 8 - PageIndex fallback | Bổ sung lớp tích hợp PageIndex an toàn, cache document ID và xử lý graceful khi provider hoặc API key không khả dụng. | `src/task8_pageindex_vectorless.py`, commit `d6c9c65` | Hoàn thành |
| Task 9 - Retrieval pipeline | Kết nối dense search, lexical search, RRF và PageIndex fallback; dùng điểm dense gốc để quyết định fallback và bắt lỗi provider. | `src/task9_retrieval_pipeline.py`, commits `d6c9c65`, `5bbc648`, `be86cf4` | Hoàn thành |
| Task 10 - Generation | Triển khai reorder context, format context có title/source để citation, safe refusal và gọi Gemini qua `google-genai` với model `gemini-3.8-flash`. | `src/task10_generation.py`, commit `d6c9c65` | Hoàn thành |
| Xử lý merge conflict | Giải quyết conflict của Task 9, đồng bộ với `origin/main` và giữ lại bản báo cáo tiếng Việt trên branch cá nhân. | `reports/INDIVIDUAL_REPORT.md`, commit `be86cf4` | Hoàn thành |

## Các quyết định kỹ thuật quan trọng

1. **Dùng RRF thay vì cộng trực tiếp điểm dense và lexical.** Mỗi tài liệu nhận điểm `1 / (k + rank)` từ từng bảng xếp hạng. Cách này tránh trộn hai thang điểm khác nhau và vẫn giữ được các tài liệu xuất hiện ở nhiều nguồn.

2. **Dùng điểm dense gốc để quyết định fallback.** Task 9 so sánh điểm dense cao nhất với `SCORE_THRESHOLD`, không dùng điểm RRF cho threshold vì RRF chỉ phản ánh thứ hạng.

## Kiểm thử và kết quả

- Đã kiểm tra cú pháp các module Task 7–10 bằng `python -m py_compile`.
- Đã đối chiếu chữ ký hàm và schema kết quả với `docs/MODULE_CONTRACTS.md` và `src/contracts.py`.
- Đã kiểm tra các trường hợp RRF loại trùng, giới hạn `top_k`, fallback theo điểm dense và lỗi PageIndex.
- Đã cấu hình Task 10 với `LLM_PROVIDER=gemini`, `GEMINI_API_KEY` và `LLM_MODEL=gemini-3.8-flash`.
- Đã push branch cá nhân lên `origin/minh`; commit mới nhất là `be86cf4`.
- Không commit API key hoặc response từ provider bên ngoài.

## Điều còn hạn chế và hướng phát triển

- Hybrid retrieval tăng khoảng 50 ms cho mỗi query do thêm bước lexical search và RRF.
- Các câu hỏi chứa số liệu cụ thể hoặc câu hỏi ngoài phạm vi corpus vẫn có điểm đánh giá thấp; có thể cải thiện bằng cách mở rộng corpus và tối ưu chunking/BM25.

## Xác nhận đóng góp

Tôi xác nhận báo cáo này phản ánh đúng các phần việc trực tiếp đã thực hiện và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Phạm Anh Minh
